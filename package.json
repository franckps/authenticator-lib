{
  "name": "@franciscodev.pro/authenticator",
  "version": "1.0.0",
  "private": false,
  "description": "",
  "main": "build/index.js",
  "types": "build/index.d.js",
  "files": [
    "build",
    "README.md"
  ],
  "scripts": {
    "test": "jest --passWithNoTests --silent --noStackTrace --runInBand --no-cache",
    "test:verbose": "jest --passWithNoTests --watch --runInBand",
    "test:unit": "npm run test -- --watch -c jest-unit-config.js",
    "test:integration": "jest --passWithNoTests --runInBand -c jest-integration-config.js",
    "test:staged": "npm run test -- --findRelatedTests",
    "test:coverage": "jest --passWithNoTests --silent --noStackTrace --no-cache --coverage",
    "build": "npx tsc"
  },
  "keywords": [],
  "author": "franciscodev.pro",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/jest": "^29.5.12",
    "@types/node": "^20.12.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
